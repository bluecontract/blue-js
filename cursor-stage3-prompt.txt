Implement Stage 3 of the BLUE TypeScript DSL SDK in this repo.

Read first, in this order:
- AGENTS.md
- .cursor/rules/blue-ts-dsl-sdk-stage3.mdc
- .cursor/rules/blue-ts-dsl-sdk-stage3-testing.mdc
- docs/ts-dsl-sdk/final-dsl-sdk-mapping-reference.md
- docs/ts-dsl-sdk/stage-3-spec.md
- docs/ts-dsl-sdk/stage-3-implementation-plan.md
- docs/ts-dsl-sdk/stage-3-testing-strategy.md
- docs/ts-dsl-sdk/stage-3-mapping-matrix.md
- docs/ts-dsl-sdk/stage-3-coverage-matrix.md
- docs/ts-dsl-sdk/stage-3-deviations.md

Use Java references as the secondary parity source for API shape and scenario discovery:
- references/java-sdk/docs/sdk-dsl-developers.md
- references/java-sdk/docs/sdk-dsl-mapping-audit-reference.md
- references/java-sdk/src/main/java/blue/language/sdk/DocBuilder.java
- references/java-sdk/src/main/java/blue/language/sdk/internal/StepsBuilder.java
- references/java-sdk/src/test/java/blue/language/sdk/**

This prompt is stored in blue-js/cursor-stage3-prompt.txt for future reference.


Important:
Search the Java test tree and docs for stage-3-relevant MyOS/admin/session-interaction scenarios.
Port those scenarios as closely as possible for the Stage 3 scope.
Do not invent a stage-3 scope wider than what is documented here.

Source-of-truth policy for Stage 3:
- `docs/ts-dsl-sdk/final-dsl-sdk-mapping-reference.md` is the primary source of truth for Conversation/MyOS/PayNote domain mappings.
- For Stage 3 specifically, use that mapping document as authoritative for MyOS/Admin/session-interaction event shapes, channel types, wrappers, and bootstrap-related semantics.
- Java references are the secondary source of truth for public API shape, naming, fluent ergonomics, and scenario intent.
- Existing TypeScript runtime behavior in `libs/language` and `libs/document-processor` is the final execution gate.
- If a proven conflict appears between Java parity and the final mapping reference for an in-scope feature, prefer the final mapping reference plus runtime-correct behavior, document the mismatch in `docs/ts-dsl-sdk/stage-3-deviations.md`, and add a focused regression test.

Current assumptions:
- Stage 1 and Stage 2 already exist on this branch.
- You must keep stage-1 and stage-2 behavior green.
- Do not rewrite the SDK from scratch.

Mapping sections you must use directly during implementation:
- `final-dsl-sdk-mapping-reference.md` sections:
  - 2.1 explicit channels in runtime documents
  - 2.3 no `Common/Named Event` in current repo
  - 3.4 pending actions and customer interactions
  - 3.5 bootstrap-related Conversation events
  - 4.3 `MyOS/MyOS Timeline Channel`
  - 4.5 `Core/Triggered Event Channel`
  - 5.1 `MyOS/MyOS Admin Base`
  - 5.2 session interaction request/response wrappers
  - 5.4 subscribe-to-session and subscription update envelope
  - 5.5 call-operation request and response forwarding
  - 5.9 `MyOS/Document Session Bootstrap`
  - 8.1 Stage 3 implications

Stage 3 scope:
1. `DocBuilder.myOsAdmin(...)`
2. triggered-event matcher helpers:
   - `onTriggeredWithId(...)`
   - `onTriggeredWithMatcher(...)`
   - `onSubscriptionUpdate(...)`
   - `onMyOsResponse(...)`
3. `StepsBuilder.myOs()` / `MyOsSteps`
4. MyOS request helper builders for:
   - single-document permission request
   - subscribe-to-session request
   - call-operation request
5. stage-3 parity tests
6. stage-3 processor-backed integration tests
7. stage-3 docs / matrices

Out of scope:
- `access()`
- `accessLinked()`
- `agency()`
- AI
- payments / PayNote implementation
- DocStructure / DocPatch / BlueChangeCompiler
- worker-agency helpers
- participants orchestration helpers
- linked-doc grant orchestration helpers
- dependency upgrades unless absolutely required and clearly justified

Non-negotiable rules:
- Do not change `document-processor`
- Do not weaken stage-1/stage-2 tests
- Do not widen scope to stage 4+
- Do not replace mapping-reference-derived scenarios with easier TS-only scenarios if the mapped scenarios are feasible here
- Do not import `document-processor` into the runtime library surface
- Do not claim completion if verification was not actually run

Implementation tasks:

A. Baseline verification
- run:
  - `npm install`
  - `npx tsc -p libs/sdk-dsl/tsconfig.lib.json --noEmit`
  - `npx tsc -p libs/sdk-dsl/tsconfig.spec.json --noEmit`
  - `npx eslint libs/sdk-dsl`
  - `npx vitest run --config libs/sdk-dsl/vite.config.ts`
  - `npx vite build --config libs/sdk-dsl/vite.config.ts`
- if baseline is red, report it clearly and only fix issues that block stage-3 work or were introduced by your changes

B. Implement `myOsAdmin(...)`
- add the Stage 3 helper to `DocBuilder`
- materialize the standard admin-delivery contracts needed for non-admin document types according to mapping reference section 5.1:
  - `myOsAdminChannel`
  - `myOsAdminUpdate`
  - `myOsAdminUpdateImpl` that re-emits delivered events
- use `MyOS/MyOS Timeline Channel`
- keep root document `type` unchanged
- if the document already uses `type: MyOS/MyOS Admin Base`, do not duplicate inherited contracts unless an explicit extension is needed
- add parity coverage
- add runtime coverage proving re-emission works

C. Implement triggered-event matcher helpers
- add Java-equivalent overloads/signatures for:
  - `onTriggeredWithId(...)`
  - `onTriggeredWithMatcher(...)`
  - `onSubscriptionUpdate(...)`
  - `onMyOsResponse(...)`
- bind them to the runtime-correct triggered-event channel
- use mapping-reference-confirmed wrappers and response envelopes
- do not assume `Common/Named Event`
- port stage-3-relevant Java tests or reproduce them closely, but align matcher payloads to the final mapping reference

D. Implement `steps.myOs()` / `MyOsSteps`
- add a stage-3 namespace entry on `StepsBuilder`
- implement thin helper builders for the standard requested-event flows needed in the stage-3 scenarios:
  - `MyOS/Single Document Permission Grant Requested`
  - `MyOS/Subscribe to Session Requested`
  - `MyOS/Call Operation Requested`
- build runtime-correct event payloads from the mapping reference; do not invent a separate abstraction layer
- keep helpers thin wrappers over existing Stage 2 trigger/emit mechanisms

E. Add runtime integration tests
At minimum add processor-backed tests for:
1. admin update re-emits delivered events
2. subscription update handler filters by `subscriptionId`
3. response matcher flow triggers only for the intended MyOS response shape (`MyOS/Call Operation Responded` wrapper or mapped response envelope as applicable)
4. non-admin document can still participate in an admin/session-interaction flow when `myOsAdmin(...)` is present
5. one end-to-end counter/session-interaction scenario if supported cleanly by current runtime

F. Update docs
- fill in:
  - `docs/ts-dsl-sdk/stage-3-mapping-matrix.md`
  - `docs/ts-dsl-sdk/stage-3-coverage-matrix.md`
  - `docs/ts-dsl-sdk/stage-3-deviations.md`
- keep docs honest; only document real deviations
- when a deviation is due to Java POC drift, say so explicitly

Testing rules:
- parity should compare canonical Blue trees after preprocess using `official` JSON
- use BlueId comparison where practical
- YAML fixtures may remain readable expected documents, but raw YAML formatting is not the primary oracle
- keep tests readable with short clarifying comments and properly indented fluent chains

Formatting rule:
Prefer readable multiline fluent chains, for example:
`.myOsAdmin('myOsAdminChannel')`
`  .onSubscriptionUpdate(...)`
`  .operation('increment')`
`    .channel('ownerChannel')`
`    .done()`

Definition of done for Stage 3:
1. stage-3 scope implemented
2. stage-3 scenarios derived from the final mapping reference are covered
3. runtime tests green
4. stage-1 and stage-2 verification still green
5. docs/matrices updated
6. final response includes:
   - exact files changed
   - implemented features
   - tests added/updated
   - remaining deviations
   - exact verification commands run

Do the work directly.
Do not ask broad questions.
If a real runtime conflict appears, document it and continue within scope.
