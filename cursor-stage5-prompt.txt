Implement Stage 5 of the BLUE TypeScript DSL SDK in this repo.

Read first, in this order:
- AGENTS.md
- .cursor/rules/blue-ts-dsl-sdk-stage5.mdc
- .cursor/rules/blue-ts-dsl-sdk-stage5-testing.mdc
- docs/ts-dsl-sdk/final-dsl-sdk-mapping-reference.md
- docs/ts-dsl-sdk/stage-5-spec.md
- docs/ts-dsl-sdk/stage-5-implementation-plan.md
- docs/ts-dsl-sdk/stage-5-testing-strategy.md
- docs/ts-dsl-sdk/stage-5-mapping-matrix.md
- docs/ts-dsl-sdk/stage-5-coverage-matrix.md
- docs/ts-dsl-sdk/stage-5-deviations.md
- docs/ts-dsl-sdk/canonical-scenarios/ai-provider-patterns.md

Use Java references as the secondary parity source for API shape and scenario discovery:
- references/java-sdk/docs/sdk-dsl-developers.md
- references/java-sdk/docs/sdk-dsl-mapping-audit-reference.md
- references/java-sdk/docs/sdk-dsl-mapping-reference.md
- references/java-sdk/src/main/java/blue/language/sdk/DocBuilder.java
- references/java-sdk/src/main/java/blue/language/sdk/internal/StepsBuilder.java
- references/java-sdk/src/test/java/blue/language/sdk/dsl/DocBuilderAiDslParityTest.java
- references/java-sdk/src/test/java/blue/language/samples/sdk/DocBuilderExamples.java
- references/java-sdk/src/test/java/blue/language/samples/sdk/MyOsCookbookExamples.java
- references/java-sdk/src/test/java/blue/language/sdk/**

This prompt is stored in blue-js/cursor-stage5-prompt.txt for future reference.

Important:
Search the Java source and test tree for stage-5-relevant APIs and scenarios around:
- `ai(`
- `task(`
- `permissionFrom(`
- `statusPath(`
- `contextPath(`
- `requesterId(`
- `requestPermissionOnInit(`
- `requestPermissionOnEvent(`
- `requestPermissionOnDocChange(`
- `requestPermissionManually(`
- `askAI(`
- `steps.ai(...).requestPermission(`
- `steps.ai(...).subscribe(`
- `onAIResponse(`

Port those scenarios as closely as possible for the Stage 5 scope.
Do not invent a stage-5 scope wider than what is documented here.

Source-of-truth policy for Stage 5:
- `docs/ts-dsl-sdk/final-dsl-sdk-mapping-reference.md` is the primary source of truth for Conversation/MyOS/AI/PayNote domain mappings.
- For Stage 5 specifically, use that mapping document as authoritative for:
  - AI / LLM-provider orchestration,
  - provider request/response payload shapes,
  - caller-side permission / subscribe / call-operation / response-handling flows,
  - `Conversation/Request`, `Conversation/Response`, `Conversation/Event`, and `Conversation/Chat Message` usage,
  - `requestId` / requester / correlation semantics,
  - named-event behavior constraints,
  - auto context persistence expectations.
- Java references are the secondary source of truth for public API shape, naming, fluent ergonomics, nested builder behavior, and scenario intent.
- Existing TypeScript runtime behavior in `libs/language` and `libs/document-processor` is the final execution gate.
- `docs/ts-dsl-sdk/canonical-scenarios/ai-provider-patterns.md` is the acceptance corpus for real AI/provider orchestration scenarios.
- At least one Stage 5 scenario must be reconstructed from that canonical AI provider scenario corpus and proven through processor-backed runtime tests.
- If a proven conflict appears between Java parity and the final mapping reference for an in-scope feature, prefer the final mapping reference plus runtime-correct behavior, document the mismatch in `docs/ts-dsl-sdk/stage-5-deviations.md`, and add a focused regression test.

Current assumptions:
- Stage 1, Stage 2, Stage 3, and Stage 4 already exist on this branch and are green.
- You must keep stage-1/stage-2/stage-3/stage-4 behavior green.
- Do not rewrite the SDK from scratch.

Mapping sections you must use directly during implementation:
- `final-dsl-sdk-mapping-reference.md` sections:
  - 2.1 explicit channels in runtime documents
  - 2.2 `requestId`
  - 2.3 lack of `Common/Named Event` in current repo
  - 2.4 inherited contracts vs explicit duplication
  - 3.1 `Conversation/Event`, `Conversation/Request`, `Conversation/Response`, `Conversation/Chat Message`
  - 3.2 operation/workflow/step building blocks
  - 3.4 pending actions and customer interactions where AI response flows surface user-facing messages
  - 4.3 `MyOS/MyOS Timeline Channel`
  - 5.1 `MyOS/MyOS Admin Base`
  - 5.2 single-document permission request/response/control events
  - 5.4 subscribe-to-session wrappers
  - 5.5 call-operation request/response forwarding
  - 6.1 key AI decision
  - 6.2 provider-side pattern
  - 6.3 caller-side orchestration pattern
  - 8.3 Stage 5 implications

Stage 5 scope:
1. `DocBuilder.ai(...)`
2. AI integration nested builder and `.done()`
3. AI task nested builder(s) and `.done()` if applicable from Java parity
4. `StepsBuilder.askAI(...)`
5. `steps.ai(name).requestPermission(...)`
6. `steps.ai(name).subscribe(...)`
7. `DocBuilder.onAIResponse(...)` overloads:
   - default response type matcher
   - explicit response type matcher
   - task-filtered matcher
   - named-event matcher where runtime mapping supports it
   - named-event + task-filtered matcher where runtime mapping supports it
8. stage-5 parity tests
9. stage-5 processor-backed integration tests
10. stage-5 docs / matrices

Out of scope:
- payments / PayNote implementation
- provider-side dedicated macro builder beyond what is already representable with generic Stage 1/2 APIs unless Java clearly defines one in scope
- patch/editing compiler work
- access/accessLinked/agency Stage 4 work except where Stage 5 composes with it
- dependency upgrades unless absolutely required and clearly justified

Non-negotiable rules:
- Do not change `document-processor`
- Do not weaken stage-1/stage-2/stage-3/stage-4 tests
- Do not widen scope to stage 6+
- Do not replace mapping-reference-derived AI scenarios with easier TS-only scenarios if the mapped scenarios are feasible here
- Do not import `document-processor` into the runtime library surface
- Do not invent local fake repo types when `@blue-repository/types` does not provide them
- Do not claim completion if verification did not pass

What to build:
- Keep the implementation layered:
  - Stage 1: core document authoring
  - Stage 2: handlers and richer steps
  - Stage 3: MyOS/admin/session-interaction foundations
  - Stage 4: higher-level access / linked access / agency abstractions
  - Stage 5: AI / LLM-provider orchestration on top of standard session interaction and Conversation request/response payloads
- Prefer composition over duplicated low-level event-shape assembly when Stage 3 and Stage 4 helpers already exist.
- Keep runtime behavior aligned to the final mapping reference.
- Mirror the Java public API shape closely where feasible.
- Treat Stage 5 helpers as legitimate higher-level flow builders: a single DSL expression may materialize multiple contracts/workflows when that is required by the mapped AI orchestration pattern.

Required deliverables:
- implementation inside `libs/sdk-dsl`
- stage-5 parity tests
- stage-5 runtime integration tests
- updated `docs/ts-dsl-sdk/stage-5-spec.md`
- updated `docs/ts-dsl-sdk/stage-5-implementation-plan.md`
- updated `docs/ts-dsl-sdk/stage-5-testing-strategy.md`
- updated `docs/ts-dsl-sdk/stage-5-mapping-matrix.md`
- updated `docs/ts-dsl-sdk/stage-5-coverage-matrix.md`
- updated `docs/ts-dsl-sdk/stage-5-deviations.md`

Verification commands to run before claiming completion:
- `npm install`
- `npx tsc -p libs/sdk-dsl/tsconfig.lib.json --noEmit`
- `npx tsc -p libs/sdk-dsl/tsconfig.spec.json --noEmit`
- `npx eslint libs/sdk-dsl`
- `npx vitest run --config libs/sdk-dsl/vite.config.ts`
- `npx vite build --config libs/sdk-dsl/vite.config.ts`

When finished, report:
- which files were changed
- which stage-5 APIs were implemented
- which Java scenarios were ported
- which runtime-backed scenarios were proven
- what deviations remain, if any
- the exact verification commands you ran
