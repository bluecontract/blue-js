Implement Stage 6 of the BLUE TypeScript DSL SDK in this repo.

Read first, in this order:
- AGENTS.md
- .cursor/rules/blue-ts-dsl-sdk-stage6.mdc
- .cursor/rules/blue-ts-dsl-sdk-stage6-testing.mdc
- docs/ts-dsl-sdk/final-dsl-sdk-mapping-reference.md
- docs/ts-dsl-sdk/complex-flow-materialization-reference.md
- docs/ts-dsl-sdk/stage-6-spec.md
- docs/ts-dsl-sdk/stage-6-implementation-plan.md
- docs/ts-dsl-sdk/stage-6-testing-strategy.md
- docs/ts-dsl-sdk/stage-6-mapping-matrix.md
- docs/ts-dsl-sdk/stage-6-coverage-matrix.md
- docs/ts-dsl-sdk/stage-6-deviations.md
- docs/ts-dsl-sdk/canonical-scenarios/paynote-business.md

Use Java references as the secondary parity source for API shape and scenario discovery:
- references/java-sdk/docs/sdk-dsl-developers.md
- references/java-sdk/docs/sdk-dsl-mapping-audit-reference.md
- references/java-sdk/docs/sdk-dsl-mapping-reference.md
- references/java-sdk/src/main/java/blue/language/sdk/DocBuilder.java
- references/java-sdk/src/main/java/blue/language/sdk/internal/StepsBuilder.java
- references/java-sdk/src/main/java/blue/language/sdk/paynote/PayNoteBuilder.java
- references/java-sdk/src/test/java/blue/language/sdk/**
- references/java-sdk/src/test/java/blue/language/samples/sdk/**

This prompt is stored in blue-js/cursor-stage6-prompt.txt for future reference.

Important:
Search the Java source and test tree for stage-6-relevant APIs and scenarios around:
- `PayNotes.payNote(`
- `PayNotes.cardTransactionPayNote(`
- `PayNotes.merchantToCustomerPayNote(`
- `PayNotes.payNoteDelivery(`
- `PayNotes.paymentMandate(`
- `capture(`
- `reserve(`
- `release(`
- `lockOnInit(`
- `unlockOnEvent(`
- `unlockOnOperation(`
- `requestOnInit(`
- `requestOnEvent(`
- `requestOnOperation(`
- partial-request variants if present
- `steps.paynote.`
- `steps.conversation.documentBootstrap` 
- `steps.conversation.customerAction`

Port those scenarios as closely as possible for the Stage 6 scope.
Do not invent a stage-6 scope wider than what is documented here.

Source-of-truth policy for Stage 6:
- `docs/ts-dsl-sdk/final-dsl-sdk-mapping-reference.md` is the primary source of truth for document types, event payload shapes, and repo/runtime-confirmed field semantics.
- `docs/ts-dsl-sdk/complex-flow-materialization-reference.md` is the primary source of truth for any macro-style DSL expressions that intentionally materialize multiple contracts/workflows.
- Java references are the secondary source of truth for public API shape, naming, fluent ergonomics, nested builder behavior, and scenario intent.
- Existing TypeScript runtime behavior in `libs/language` and `libs/document-processor` is the final execution gate.
- `docs/ts-dsl-sdk/canonical-scenarios/paynote-business.md` is the acceptance corpus for real PayNote business/runtime scenarios.
- At least one Stage 6 scenario per major PayNote area must be reconstructed from that canonical corpus and proven through processor-backed runtime tests.
- If a proven conflict appears between Java parity and the final mapping references for an in-scope feature, prefer the final mapping references plus runtime-correct behavior, document the mismatch in `docs/ts-dsl-sdk/stage-6-deviations.md`, and add a focused regression test.

Current assumptions:
- Stage 1, Stage 2, Stage 3, Stage 4, and Stage 5 already exist on this branch and are green.
- You must keep stage-1/stage-2/stage-3/stage-4/stage-5 behavior green.
- Do not rewrite the SDK from scratch.

Mapping sections you must use directly during implementation:
- `final-dsl-sdk-mapping-reference.md` sections:
  - 2.1 explicit channels in runtime documents
  - 2.2 `requestId`
  - 2.4 inherited contracts vs explicit duplication
  - 2.5 bootstrap payload vs bootstrap request event vs bootstrap document
  - 3.4 pending actions and customer interactions
  - 3.5 bootstrap-related Conversation events
  - 5.9 `MyOS/Document Session Bootstrap`
  - 7.1–7.6 PayNote mappings
  - 8.4 Stage 6 implications
- `complex-flow-materialization-reference.md` sections:
  - 1 global rules
  - 3 Stage-6 materialization rules

Stage 6 scope:
1. typed PayNote document builders
2. typed paynote event helpers
3. typed conversation bootstrap/customer-action event helpers needed by paynote/delivery flows
4. macro-style PayNote action-flow builders that are confirmed by the provided references
5. stage-6 parity tests
6. stage-6 processor-backed integration tests
7. stage-6 docs / matrices

Out of scope:
- patch/editing compiler work
- stage 7+
- inventing new payment backend semantics
- widening Stage 5 AI scope
- changing `document-processor`
- dependency upgrades unless absolutely required and clearly justified

Non-negotiable rules:
- Do not change `document-processor`
- Do not weaken stage-1/stage-2/stage-3/stage-4/stage-5 tests
- Do not widen scope beyond Stage 6
- Do not duplicate inherited contracts from repo types without a documented runtime-confirmed reason
- Do not invent local fake repo types or fake repo-native fields
- Do not claim completion if verification did not pass

What to build:
- Keep the implementation layered:
  - Stage 1: core document authoring
  - Stage 2: handlers and richer steps
  - Stage 3: MyOS/admin/session foundations
  - Stage 4: higher-level access / linked access / agency abstractions
  - Stage 5: AI orchestration
  - Stage 6: payments / PayNote DSL on top of those foundations
- Use typed event helpers for payload safety and DX.
- Use macro-style materialization only when the complex-flow reference says it is valid.
- Mirror the Java public API shape closely where feasible.

Required deliverables:
- implementation inside `libs/sdk-dsl`
- stage-6 parity tests
- stage-6 runtime integration tests
- updated `docs/ts-dsl-sdk/stage-6-spec.md`
- updated `docs/ts-dsl-sdk/stage-6-implementation-plan.md`
- updated `docs/ts-dsl-sdk/stage-6-testing-strategy.md`
- updated `docs/ts-dsl-sdk/stage-6-mapping-matrix.md`
- updated `docs/ts-dsl-sdk/stage-6-coverage-matrix.md`
- updated `docs/ts-dsl-sdk/stage-6-deviations.md`
- updated `docs/ts-dsl-sdk/complex-flow-materialization-reference.md` only if a justified correction is required

Verification commands to run before claiming completion:
- `npm install`
- `npx tsc -p libs/sdk-dsl/tsconfig.lib.json --noEmit`
- `npx tsc -p libs/sdk-dsl/tsconfig.spec.json --noEmit`
- `npx eslint libs/sdk-dsl`
- `npx vitest run --config libs/sdk-dsl/vite.config.ts`
- `npx vite build --config libs/sdk-dsl/vite.config.ts`

When finished, report:
- which files were changed
- which stage-6 APIs were implemented
- which Java scenarios were ported
- which runtime-backed scenarios were proven
- which macro-style builders were implemented and how they materialize
- what deviations remain, if any
- the exact verification commands you ran
