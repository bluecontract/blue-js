package blue.language.processor.model;

import blue.language.model.TypeBlueId;

@TypeBlueId({
        "MyOS/MyOS Worker Agency",
        "MyOS Worker Agency"
})
public class MyOSWorkerAgencyMarker extends MarkerContract {
}
