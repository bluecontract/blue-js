import { schemaAnnotations } from './schema-annotations.js';

describe('schemaAnnotations', () => {
  it('should work', () => {
    expect(schemaAnnotations()).toEqual('schema-annotations');
  });
});
