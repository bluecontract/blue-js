# quickjs-wasmfile-release-sync-gas

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build quickjs-wasmfile-release-sync-gas` to build the library.

## Running unit tests

Run `nx test quickjs-wasmfile-release-sync-gas` to execute the unit tests via [Vitest](https://vitest.dev/).
