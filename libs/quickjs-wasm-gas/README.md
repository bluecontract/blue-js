# quickjs-wasm-gas

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build quickjs-wasm-gas` to build the library.

## Running unit tests

Run `nx test quickjs-wasm-gas` to execute the unit tests via [Vitest](https://vitest.dev/).
