import { documentProcessor } from './document-processor.js';

describe('documentProcessor', () => {
  it('should work', () => {
    expect(documentProcessor()).toEqual('document-processor');
  });
});
