id: patchGasBytesUseRuntimeInsertionNormalization
category: Normalization
operation: processDocument
processorCapabilities:
  - blue-contracts-fixture-scripted-runtime-v1
initialDocument:
  contracts:
    incoming:
      type:
        blueId: C37UoAfTNUnoxkB2CdEE7BfHJwYqTNiWzQb5xuRMkBzm
    patcher:
      type:
        blueId: 2TwRC3EdLXk4gqwyyVWy52h5BQ5ntrkmpcrhrTxsGAs1
      channel: incoming
event:
  kind: patch-bare-scalar
mockRuntime:
  channels:
    - contract: /contracts/incoming
      calls:
        - when:
            event:
              kind: patch-bare-scalar
          accepted: true
          payload:
            kind: patch-bare-scalar
  handlers:
    - contract: /contracts/patcher
      calls:
        - when:
            channelKey: incoming
          result:
            patches:
              - op: replace
                path: /normalizedScalar
                val: hello
expectedStatus: success
expectedDocumentPaths:
  /normalizedScalar:
    value: hello
expectedRuntimeInsertionNormalizedValues:
  - path: /normalizedScalar
    selectedDocumentForm:
      value: hello
      type:
        blueId: GX7CFUmSDrE2MzptunLCCdZwnuwwrenRQqEnHL4x3uoC
expectedGasByteView:
  patchValuePath: /normalizedScalar
  representation: selected-document-form-after-runtime-insertion-normalization
