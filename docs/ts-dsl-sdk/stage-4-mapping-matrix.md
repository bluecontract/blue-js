# BLUE TS DSL SDK — Stage 4 mapping matrix

Primary mapping sources for all rows:
- `docs/ts-dsl-sdk/final-dsl-sdk-mapping-reference.md`
- `docs/ts-dsl-sdk/complex-flow-materialization-reference.md`

| Feature | Mapping reference | Java reference role | TS API / helper | Runtime artifact | Tests | Status | Notes |
|---|---|---|---|---|---|---|---|
| Single-document access builder | 5.2, 5.4, 5.10, 8.2 | `DocBuilderInteractionsDslParityTest` scenario source | `DocBuilder.access(...)` | request workflow + granted/rejected/revoked workflows + optional subscribe / subscription-ready / subscription-failed workflows | `DocBuilder.interactions.parity.test.ts`, `DocBuilder.interactions.integration.test.ts` | Done | Auto-ensures Stage 3 admin support contracts |
| Access timing variants | 5.2, 8.2 | Java timing APIs and parity scenarios | `.requestPermissionOnInit()`, `.requestPermissionOnEvent(...)`, `.requestPermissionOnDocChange(...)`, `.requestPermissionManually()` | init-lifecycle, triggered-event, doc-update, or manual request entry point | `DocBuilder.interactions.parity.test.ts` | Done | Manual mode only registers config; no request workflow is materialized |
| Access callbacks | 5.2, 5.4, 5.5 | Java fluent ergonomics | `onAccessGranted(...)`, `onAccessRejected(...)`, `onAccessRevoked(...)`, `onUpdate(...)`, `onCallResponse(...)` | thin wrappers over Stage 3 matchers | `DocBuilder.interactions.integration.test.ts` plus Stage 3 matcher suites | Done | Representative runtime coverage uses granted and subscription-update flows |
| Linked-documents access builder | 5.3, 5.6, 5.10, 8.2 | `DocBuilderInteractionsDslParityTest` scenario source | `DocBuilder.accessLinked(...)` | linked-doc request workflow + granted/rejected/revoked workflows | `DocBuilder.interactions.parity.test.ts`, `DocBuilder.interactions.integration.test.ts` | Done | Link anchors map to properties of `MyOS/Linked Documents Permission Set` |
| Linked-access callbacks | 5.3, 5.6 | Java fluent ergonomics | `onLinkedAccessGranted(...)`, `onLinkedAccessRejected(...)`, `onLinkedAccessRevoked(...)` | thin wrappers over response matchers | `DocBuilder.interactions.parity.test.ts` via generated flows | Done | Correlation remains `inResponseTo.requestId` |
| Agency builder | 5.8, 5.10, 8.2 | `DocBuilderInteractionsDslParityTest` scenario source | `DocBuilder.agency(...)` | worker-agency request workflow + granted/rejected/revoked workflows + `workerAgency` marker contract | `DocBuilder.interactions.parity.test.ts`, `DocBuilder.interactions.integration.test.ts` | Done | Uses runtime-correct `allowedWorkerAgencyPermissions` list |
| Agency/session callbacks | 5.8 | Java fluent ergonomics | `onAgencyGranted(...)`, `onAgencyRejected(...)`, `onAgencyRevoked(...)`, `onSessionStarting(...)`, `onSessionStarted(...)`, `onSessionFailed(...)`, `onParticipantResolved(...)`, `onAllParticipantsReady(...)` | thin wrappers over triggered-event or response matchers | `DocBuilder.interactions.integration.test.ts` plus Stage 3 matcher suites | Done | Representative runtime coverage uses `onAgencyGranted(...)` |
| Access step namespace | 5.2, 5.4, 5.5 | Java `StepsBuilder` scenario source | `steps.access(accessName)` | composed request / call-operation / subscribe / revoke steps | `DocBuilder.interactions.parity.test.ts` | Done | Requires a previously registered `access(...)` config |
| Agency step namespace | 5.8 | Java `StepsBuilder` scenario source | `steps.viaAgency(agencyName)` | composed worker-agency request and worker-session start steps | `DocBuilder.interactions.parity.test.ts`, `DocBuilder.interactions.integration.test.ts` | Done | Requires a previously registered `agency(...)` config |
| Low-level MyOS stage-4 request/control helpers | 5.2, 5.3, 5.8 | `DocBuilderMyOsDslParityTest` scenario source | `steps.myOs().requestLinkedDocsPermission(...)`, `.revokeSingleDocPermission(...)`, `.revokeLinkedDocsPermission(...)`, `.grantWorkerAgencyPermission(...)`, `.revokeWorkerAgencyPermission(...)`, `.startWorkerSession(...)` | runtime-correct trigger-event payloads | `StepsBuilder.myos.test.ts`, `DocBuilder.interactions.parity.test.ts` | Done | Kept as thin wrappers over Stage 2 trigger-event construction |
| Generated contract section tracking | 2.1, 2.4, 8.2 | repository behavior requirement, not a Java-exclusive feature | interaction builders inside `.section(...)` | generated access / linked-access / agency contracts added to `relatedContracts` | `DocBuilder.interactions.parity.test.ts` | Done | Extends Stage 1 section semantics instead of bypassing them |
| Runtime-vs-Java drift cases | deviations doc | comparison baseline only | documented deviations | focused regression coverage | `DocBuilder.interactions.parity.test.ts`, `StepsBuilder.myos.test.ts`, `DocBuilder.interactions.integration.test.ts` | Done | Only real runtime-confirmed deviations are recorded |
